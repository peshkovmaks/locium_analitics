=== РЕЗЮМЕ ПРОЕКТА: Multi-Marketplace Analytics Dashboard ===

КОНТЕКСТ:
- Пользователь продаёт на 3 площадках: Wildberries, Ozon, Яндекс Маркет
- 20 SKU (кухонные товары)
- Нужен аналитический дашборд с юнит-экономикой

УЖЕ СДЕЛАНО (фронтенд):
1. Полностью интерактивный дашборд с 8 блоками:
   - KPI-шапка (выручка, валовая, чистая, ДРР) + WoW
   - Алерты (цена < минимальной, остаток < 10, ДРР > 12%, маржа < 15%)
   - Линейный график выручки по 3 площадкам
   - Donut: доля выручки по площадкам
   - Donut: структура расходов (комиссия, логистика, хранение, реклама, возвраты, прочее)
   - DRR-бары с цветовой индикацией
   - Блок "Заработок с 1 товара" — юнит-экономика по каждому SKU на каждой площадке
   - Таблица 20 товаров с поиском, сортировкой, статусами
   - Сравнение площадок (итоговая таблица)

2. Формулы зафиксированы:
   - Валовая = Выручка − (Комиссия + Логистика + Хранение + Реклама + Возвраты + Прочее)
   - Чистая = Валовая − (Себестоимость × Кол-во)
   - ДРР = Реклама / Выручка × 100%

3. Технологии фронта: React 19 + TypeScript + Tailwind CSS + Recharts
   (есть также standalone HTML-версия)

ЧТО НУЖНО СДЕЛАТЬ (бэкенд):
1. FastAPI проект с Docker Compose
   - PostgreSQL (пользователи, роли, магазины, каталог, товары)
   - Redis (сессии, кэш)
   - Celery (фоновый сбор данных каждые 4 часа)

2. Адаптеры для 3 площадок (Adapter Pattern):
   - [x] WB API (статистика, реклама, остатки, финансы)
   - [x] Ozon Seller API
   - [~] Яндекс Маркет Partner API (заказы/остатки работают; детальные расходы уточняются)

3. Модели БД:
   - users (id, email, password_hash, role: admin/viewer)
   - shops (id, user_id, marketplace, name, credentials JSONB encrypted)
   - products (id, user_id, sku, name, cost_price, min_price, category)
   - shop_products (id, shop_id, product_id, external_sku, external_id)
   - raw_sales / raw_stocks / raw_adverts (ClickHouse или PostgreSQL)

4. Telegram-интеграция:
   - Ежевечерний отчёт в 21:00
   - Алерты: цена < минимальной, ДРР > порога, падение прибыли

5. Авторизация: JWT + роли (Admin = полный доступ, Viewer = только просмотр)

КЛЮЧЕВЫЕ РЕШЕНИЯ:
- Период по умолчанию: "Сегодня"
- Обновление: раз в 4 часа
- Валюта: рубли
- Себестоимость: вводится вручную в разделе "Каталог"
- Минимальная цена: задаётся в Каталоге, сравнивается с ценой на витрине
- Возвраты: отдельной строкой

СЛЕДУЮЩИЙ ШАГ:
Начать с бэкенда: FastAPI + SQLAlchemy модели + Docker Compose
